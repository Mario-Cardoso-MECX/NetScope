<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NetScope" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- MISMAS REGLAS PARA EL MODO NOCHE -->
        <item name="android:statusBarColor">#0D1117</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="Theme.NetScope" parent="Base.Theme.NetScope" />
</resources>