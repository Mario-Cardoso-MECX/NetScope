<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NetScope" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- MATANDO LA BARRA ROSADA/GRIS -->
        <!-- 1. Forzamos el fondo de la barra de estado al color oscuro de la app -->
        <item name="android:statusBarColor">#0D1117</item>

        <!-- 2. Le decimos que NO es una barra clara, para que fuerce el reloj y batería a color BLANCO -->
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <style name="Theme.NetScope" parent="Base.Theme.NetScope" />
</resources>